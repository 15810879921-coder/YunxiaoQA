# Plan 确认清单

凡写云效，Plan 正文至少覆盖下列勾选项（按口令裁剪）。

## 通用头

- [ ] 云效项目名 + `spaceIdentifier`（新建/不确定时已 PJ 点选）
- [ ] 口令类型（开始测试 / 独立发起缺陷 / 从测试用例发起缺陷 / 再次打开 / 批量关闭 / 并入迭代 / 完成测试…）
- [ ] 涉及工作项编号列表
- [ ] **不会做**：不建【开发】/【测试】、不创建迭代、不代开发标已修复/暂不修复

## 发起缺陷 / 从测试用例发起缺陷 / 发起缺陷(非本期)

- [ ] 标题终稿
- [ ] 查重结论：新建 / 复用 `ONEOS-xx`
- [ ] **挂载点选**：【测试】已点选（或口令唯一命中并确认）；需求可选写入描述追溯（见 [anchor-selection.md](anchor-selection.md)）
- [ ] 验证者策略=`当前登录测试用户`；不接受口令覆盖；apply 后必须回报云效解析的姓名+userId和回读值
- [ ] 测试用例路径已提供用例编号/执行记录并声明使用共同`create_bug.py`处理器（独立路径不要求）
- [ ] 负责人姓名 + userId（本期：开发任务来源说明；非本期：口令指定）
- [ ] 关联：【测试】ASSOCIATED（关联项）；需求仅描述追溯（不做 ASSOCIATED API）
- [ ] 优先级（默认中；口令可改）
- [ ] 标签名称 + 实时解析的 tag identifier；创建后回读
- [ ] 附件绝对路径清单；逐个核对文件存在、名称、大小，创建后逐个上传回读
- [ ] 描述已含：现象、环境、步骤、实际/期望、初步定位

## 再次打开

- [ ] 缺陷编号 + 当前状态确认为 `已修复`
- [ ] 复现说明 / 证据摘要
- [ ] 确认不修改负责人、验证者
- [ ] 若源状态为 `暂不修复`：用户已显式确认坚持打开
- [ ] 测试结果评论终稿；复现证据附件清单（若有）；顺序=附件→评论→再次打开

## 批量关闭已修复

- [ ] 待关闭编号列表（均为 `已修复`）
- [ ] 已排除复现 / 将再次打开的编号
- [ ] 每条待关闭Bug都分别提供：复测用例、复测执行ID、证据ID/URL、test环境、已部署版本、当前验证用户
- [ ] 将逐条调用`transit_bug.py`先写入并回读`oneos.bug-retest/v1`，证据缺一项则该Bug不关闭
- [ ] 每张通过单的测试结果评论终稿；顺序=评论→复测证据→关闭
- [ ] 关闭后是否检测【测试】可闭环（是/否）

## 并入当期迭代

- [ ] 缺陷编号均为 `已关闭`
- [ ] 目标迭代名称 + sprintId（已存在；非新建）

## 开始测试

- [ ] 【测试】=`已分配`、需求=`待测试`
- [ ] 【测试】唯一父【交付】、正式关联需求与口令一致
- [ ] 已回读`oneos.test-deployment/v1`：环境=test、终态成功，项目/迭代/需求/测试任务均精确一致，且包含待测版本和执行证据
- [ ] dry-run显示两侧下一状态均唯一可达

## 完成测试

- [ ] 【测试】=`处理中`、需求=`测试中`
- [ ] 唯一父【交付】当前状态为已分配/待处理/处理中之一或已完成，目标为已完成
- [ ] 已提供真实JSON证据清单文件，schema=`oneos.qa-evidence/v1`；不接受口令逐项自报
- [ ] 清单中的项目、迭代、需求、测试任务、部署执行与待测版本均和云效受管证据精确一致
- [ ] 测试计划、用例执行、测试报告都有非空ID和可访问URL；未执行/失败/阻塞用例均为0
- [ ] 关联缺陷清单；活跃缺陷为0
- [ ] 每条已关闭Bug均有自己的`oneos.bug-retest/v1`，且复测版本等于本次test部署版本
- [ ] 每条`暂不修复`均列出批准人和批准记录ID/URL
- [ ] 将用`transit_test_lifecycle.py complete --evidence-manifest <文件>`校验文件哈希、写证据并回读，再依次推进【测试】、需求、父【交付】三侧状态
- [ ] 发布候选交接字段与幂等键
- [ ] 确认不走浏览器点状态；回报三侧`编号 | 标题 | from→to`

## 用户确认用语

接受：`确认` / `执行` / `批准计划` / `按方案执行`。
压缩点选（若 Plan 给了字母表）须先解析回显再执行。
